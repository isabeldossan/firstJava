package Övningar.från.pdf;

import java.util.Scanner;

public class onetwothreefour {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);

//1. Skapa ett program som skriver ut kvadraten på ett tal som läses in. Kvadrat är talet multiplicerat med sig själv.

       /* System.out.println("Skriv in hur lång sidorna på kvadraten är för att få ut arenan:");
        int number = Integer.parseInt(scan.nextLine());
        int area = number * number;
        System.out.println("Arean på kvadraten är: " + area);

        */
//2.  En försäljare har delvis prestationslön. Han får 8000 kr per månad i grundlön och 9% av försäljningssumman.
// Skapa ett program som beräknar lönesumman under en period. Försäljningssumman ska läsas in.
/*
        System.out.print("Skriv in försäljningssumman för denna månad:");
        int sales = Integer.parseInt(scan.nextLine());
        int percent = sales/100*9;
        int salary = percent + 8000;
        System.out.println("Du har 8000 i grundlön och 9% av månadens försäljningssumma blir " + percent + " " +
                " därför blir din lön: " + salary);
*/

//3. Skapa ett program som läser in antal timmar, beräknar och skriver ut hur mycket det
//blir omvandlat till minuter resp sekunder.

/*
        System.out.println("Skriv in antal timmar: ");
        int timmar = Integer.parseInt(scan.nextLine());
        int minuter = timmar * 60;
        int sekunder = minuter * 60;
            System.out.println(timmar + " timmar är lika med " + minuter + " minuter och " + sekunder + " sekunder");
*/
        //4. Skapa ett program som läser in tre tal och beräknar och skriver
        // ut summan samt medelvärdet av de tre talen.

        /*System.out.print("Skriv in ditt första tal: ");
        int one = Integer.parseInt(scan.nextLine());
        System.out.print("Skriv in ditt andra tal: ");
        int two = Integer.parseInt(scan.nextLine());
        System.out.print("Skriv in ditt tredje tal: ");
        int three = Integer.parseInt(scan.nextLine());
        int sum = one + two + three;
        System.out.println("Summan av dina tre tal är: " + sum + " och medelvärdet är: " + (sum/3));
*/


    }
}
