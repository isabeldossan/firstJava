package Övningar.från.pdf;

import java.util.Scanner;

public class fivesixseveneight {
    public static void main(String[] args) {

        Scanner scan = new Scanner(System.in);

        //5. Läs in ett heltal motsvarande svenska kronor och skriv ut motsvarande värde i pund respektive dollar.
        // Antag att kursen är : 1 dollar = 6 kr, 1 pund = 10 kr.

        /*System.out.print("Vänligen skriv in ett heltal i svenska kronor för att se värdet i pund & dollar: ");
        int sek = Integer.parseInt(scan.nextLine());
        int dollar = sek / 6;
        int pund = sek / 10;
        System.out.println("Du skrev in i sek: " + sek + ". Detta blir i dollar: " + dollar + " och i pund blir det: " + pund);
        */

        //6. Skapa ett program som beräknar funktionen ax3 + 7 och skriver ut resultatet. Variablerna
        //a och x ska läsas in.

        /*
        System.out.println("Skriv in ett heltal: ");
        int a = Integer.parseInt(scan.nextLine());
        System.out.println("Skriv in ett till heltal: ");
        int x = Integer.parseInt(scan.nextLine());
        System.out.print((a + x + 3) + 7);
        */

        //7. Skapa ett program som beräknar vad du ska betala för en tank bensin. Indata är antal
        //liter, pris per liter och eventuell rabatt i procent. Utdata är priset som du ska betala

        /*System.out.print("Priset för en liten bensin är 16kr, skriv in hur många liter du tankat: ");
        int liter = Integer.parseInt(scan.nextLine());
        int pris = liter * 16;
        System.out.println("Du ska betala i kronor: " + pris);
        */

        //8. Skapa ett program som beräknar och skriver ut arean och omkretsen av en rektangel. Rektangelns sidor ska läsas in.

        /*System.out.println("Skriv in storlek på kortsidan av rektangeln: ");
        int kort = Integer.parseInt(scan.nextLine());
        System.out.println("Skriv in storlek på långsidan av rektangeln: ");
        int lång = Integer.parseInt(scan.nextLine());

        int omkrets = (kort * 2) + (lång * 2);
        int area = kort * lång;
        System.out.println("Omkretsen är: " + omkrets + ", och arean är: " + area);
        */

    }
}
